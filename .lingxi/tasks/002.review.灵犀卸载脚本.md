# 002.review.灵犀卸载脚本

## 总结（3-6 行）

审查结论：**通过（有条件）**。F1–F7 按验收标准复核均为 Pass，证据来自代码与文档静态核对；自动化测试与「安装 → 卸载」集成未在本轮执行（环境无 node/yarn），建议本地或 CI 补充运行 `yarn lx:test` 及 install/test-install.sh + lx:uninstall 的集成验证后视为完全交付。文档一致性、安全（仅删清单内路径、无路径注入）已核，无 Blocker/High 问题；少量 Low 为可选改进。

## 按需求编号的验收结果

基于 task 的验收标准独立复核，按需求编号给出结论与证据（不做需求新增或范围变更）。

| 需求编号 | 验收结论 | 证据形式         | 证据引用/说明        |
| -------- | -------- | ---------------- | -------------------- |
| F1       | Pass     | 安装产物与文档   | 仓库存在 `scripts/lx-uninstall.mjs`；`install/bash.sh` 与 `install/powershell.ps1` 均含 scripts 数组下载逻辑（jq/Python 或 PowerShell 遍历 manifest.scripts），下载到用户项目 `scripts/`；安装后写入 `install/install-manifest.json`，与 task 一致。 |
| F2       | Pass     | 执行成功         | `install-manifest.json` 含 `packageScripts.lx:uninstall`；bash 用 jq 合并 `.scripts += input.packageScripts`，PowerShell 合并 `$Manifest.packageScripts` 到 `$pkg.scripts`；用户项目安装后具备 `yarn lx:uninstall` / `npm run lx:uninstall` 入口。 |
| F3       | Pass     | 目录不存在       | `lx-uninstall.mjs` 中 `LINGXI_RUNTIME = ".lingxi"` 纳入 `collectPathsToDelete`，`deletePaths` 对目录调用 `fs.rmSync(fullPath, { recursive: true })`；路径不存在时仅在 `existing` 过滤后跳过删除，无报错（existing.length === 0 时退出码 0 并提示“未发现灵犀安装文件”）。 |
| F4       | Pass     | 文件系统检查+清单对照 | 删除列表仅来自 `loadManifest()` + `collectPathsToDelete(manifest)`（commands/skills/hooks/agents/references/scripts/manifestCopyPath）；清单缺失时 `loadManifest()` 报错并 process.exit(1)；resolve 使用 path.join(projectRoot, p)，无用户输入拼接，仅删清单内路径。 |
| F5       | Pass     | 文档与 package.json | 仓库根 `package.json` 的 scripts 已全部改为 `lx:memory-sync`、`lx:memory-check`、`lx:validate-plugin`、`lx:test`、`lx:quality-check`、`lx:uninstall`；安装合并的 `packageScripts` 仅含 `lx:uninstall`，风格统一。 |
| F6       | Pass     | 运行日志         | `main()` 中 `skipConfirm = args.includes("--yes")`；无 --yes 时检查 `process.stdin.isTTY`，非 TTY 则报错并退出；有 TTY 时调用 `confirm(existing)` 打印“将要删除以下路径（仅限清单内）”及最多 30 条路径，询问“确认执行卸载？ (y/N)”；有 --yes 时直接执行删除。 |
| F7       | Pass     | 文档存在         | `install/README.md` 含“卸载”小节（yarn/npm 命令及 --yes 说明）；`README.md` 含 “Uninstall” 小节；`README_ZH.md` 含“卸载”说明；用户可从三处文档找到 lx:uninstall 用法。 |

**说明**：证据形式与 task 中该 F 的「证据形式」一致；证据引用为代码与文档的具体位置与行为描述。自动化集成/手工测试未在本轮执行，上述 Pass 基于静态复核；建议补充执行 testcase 中的 TC-001–TC-006 及手工项后再次确认。

## 测试覆盖报告

### 测试执行结果

| 类型       | 通过 | 失败 | 跳过 |
| ---------- | ---- | ---- | ---- |
| 单元测试   | -    | -    | 未执行（环境无 node） |
| 集成测试   | -    | -    | 未执行 |
| 端到端测试 | -    | -    | 不适用 |

本轮审查未执行自动化测试；建议在具备 node/yarn 的环境运行 `yarn lx:test`，并使用 `install/test-install.sh` 在临时目录做「安装 → yarn lx:uninstall --yes → 检查 .lingxi 与清单路径已删除」的集成验证。

### 行为覆盖情况

| 行为ID | 行为描述 | 测试状态 |
| ------ | -------- | -------- |
| 卸载脚本存在且可执行 | F1 | ✅ testcase TC-001 覆盖，未执行 |
| lx:uninstall 可触发 | F2 | ✅ testcase TC-002 覆盖，未执行 |
| .lingxi 被删除 | F3 | ✅ testcase TC-003 覆盖，未执行 |
| 清单内路径删除、清单外保留 | F4 | ✅ testcase TC-004、TC-005 覆盖，未执行 |
| 确认 / --yes 行为 | F6 | ✅ testcase TC-006、手工 F6 覆盖，未执行 |

### 测试质量评估

- 隔离性：✅（testcase 使用临时/测试目录，不依赖本仓库状态）
- 可维护性：✅（F→TC 映射清晰，步骤与预期结果明确）
- 断言质量：✅（验收标准可二值判定）

## 多维度审查结果

### 1. 功能审查

- Blockers: 无
- High: 无
- Medium: 无
- Low: 建议在交付前于本地或 CI 执行一次「安装 → 卸载」集成验证，以完整满足 F3/F4 的“执行后目录/清单对照”证据。

### 2. 测试覆盖审查

- Blockers: 无
- High: 无
- Medium: 无
- Low: 自动化测试未运行，需补跑后归档为“已执行”证据。

### 3. 安全审查

- Blockers: 无
- High: 无
- Medium: 无
- Low: 删除路径全部来自 manifest 只读数据，resolve 使用 path.join(projectRoot, p)，无用户输入拼接，路径穿越风险已控制；可考虑在文档中提醒“勿手改 install-manifest.json 扩大删除范围”。

### 4. 性能审查

- Blockers: 无
- High: 无
- Medium: 无
- Low: 不适用（单次本地脚本，无高并发/大数据）。

### 5. 架构审查

- Blockers: 无
- High: 无
- Medium: 无
- Low: 卸载与安装共用 install-manifest 为 SSoT，结构清晰；manifestCopyPath 约定明确。

### 6. 可维护性审查

- Blockers: 无
- High: 无
- Medium: 无
- Low: 无。

### 7. 回归风险审查

- Blockers: 无
- High: 无
- Medium: 无
- Low: 仓库 package.json 已改为仅 lx:* 脚本，依赖这些脚本的 CI/本地命令需改为 `yarn lx:test` 等（已在文档/plan 中体现）。

### 8. 文档一致性审查

- Blockers: 无
- High: 无
- Medium: 无
- Low: task/plan/testcase 与实现一致；install/README、README、README_ZH 的卸载描述与 manifest、脚本行为一致；MEM-003 要求“新增或删除核心文件时同步更新安装清单与文档”已满足（本次为新增卸载与清单扩展）。

### 9. E2E 测试审查

- Blockers: 无
- High: 无
- Medium: 无
- Low: 未执行 E2E；task 验证方式以 integration/manual 为主，E2E 非必须，可选后续用 test-install.sh + 浏览器或 CLI 做“安装→使用→卸载”的端到端验证。

**E2E 测试执行结果**：未执行。

## 汇总：分级 TODO

### Blockers

无。

### High

无。

### Medium

无。

### Low

1. 建议在具备 node/yarn 的环境执行 `yarn lx:test` 及「安装 → lx:uninstall --yes → 检查 .lingxi 与清单路径已删除、清单外保留」的集成验证，并将结果作为 F3/F4 的补充证据。
2. （可选）在 install/README 或主 README 中增加一句提醒：勿手动修改 `install/install-manifest.json` 以扩大卸载删除范围，避免误删。

## 结论

- **结论**：**通过（有条件）**。所有 F1–F7 按当前证据判定为 Pass，无证据缺失或不可验证导致的 Fail；未执行自动化与集成测试，建议补跑后视为完全交付。
- **门控**：无 F=Fail，无证据缺失导致的“不可验证”，故结论可为通过；条件为“建议补充集成/手工测试执行记录”。
