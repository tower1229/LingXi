# Historical And Migration Tests

This directory contains tests that still protect `.cursor`-era behavior.

They are not the current LingXi 2.0 product suite.

Use:

- `npm test` for the supported 2.0 product suite
- `npm run test:legacy` for historical migration guardrails
- `npm run test:all` when changing repository-shape or retirement work
