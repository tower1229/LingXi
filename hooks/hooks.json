{
  "description": "LingXi plugin hooks: heartbeat and session lifecycle",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/heartbeat-trigger.mjs --ide claude"
          }
        ]
      }
    ]
  }
}